cmake_minimum_required(VERSION 2.8.9)
project (curses)
set (CURSES_NEED_NCURSES TRUE)
find_package(curses)
file(GLOB SOURCES "src/*.c")
add_executable(curses ${SOURCES})
include_directories(${CURSES_INCLUDE_DIR})
target_link_libraries(curses ${CURSES_LIBARIES})